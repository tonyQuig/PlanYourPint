"use strict";

angular.module("pyp.mapPlotting")
    .service("MapService", function ($firebaseAuth, $location, $rootScope) {

        this.calculateAverage = function () {
            return console.log('Inside calculate average!');
        };

    });